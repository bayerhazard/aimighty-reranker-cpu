---
olaresManifest.version: '0.10.0'
olaresManifest.type: app
apiVersion: 'v2'
metadata:
  name: aimrerqwen3vino
  icon: https://raw.githubusercontent.com/bayerhazard/aimighty-reranker/main/icon.png
  description: Qwen3-Reranker-0.6B INT8 via OpenVINO
  appid: aimrerqwen3vino
  title: Qwen3 Reranker 0.6B CPU
  version: '0.1.2'
  categories:
    - AI
entrances:
  - name: aimrerqwen3vino
    port: 8080
    host: aimrerqwen3vino
    title: Qwen3 Reranker 0.6B CPU
    icon: https://raw.githubusercontent.com/bayerhazard/aimighty-reranker/main/icon.png
    openMethod: window
    authLevel: internal
spec:
  versionName: "0.1.2"
  upgradeDescription: |
    v0.1.2: Fixed YAML indentation in upgradeDescription (broken block scalar).
    v0.1.1: Updated ghcr.io image path to repo-scoped for public anonymous pull access.
    v0.1.0: Initial release of OpenVINO CPU-based Qwen3-Reranker-0.6B. Completely offloaded from GPU to Intel Core Ultra 9 275HX.
  fullDescription: |
    Qwen3-Reranker-0.6B + OpenVINO on CPU, optimized for Olares One.

    **Model**
    tomaarsen/Qwen3-Reranker-0.6B-seq-cls — a 600-million-parameter cross-encoder sequence classification model, INT8 quantized via Optimum Intel for efficient CPU inference.

    **Inference Engine**
    OpenVINO 2026.0.0+ with Hugging Face Transformers 4.55.4. Runs on Intel Core Ultra 9 275HX (Meteor Lake) CPU using Redwood/Golden Cove P-Cores.

    **Key Features**
    - Jina/Cohere-compatible API endpoints: /v1/rerank, /rerank, /v1/models, /health
    - Dynamic shapes, async endpoints, and multicore scaling using OpenVINO TBB
    - Automatic model download and OpenVINO conversion on first start
    - Built-in HTML dashboard on root endpoint (/)

    **Resource Usage**
    RAM: 12 GB, CPU: 4 cores
    Disk: 10 GB

  developer: Aimighty
  website: https://github.com/bayerhazard/aimighty-reranker-cpu
  sourceCode: https://github.com/bayerhazard/aimighty-reranker-cpu
  locale:
    - en-US
  supportArch:
    - amd64
  limitedCpu: 4
  requiredCpu: 1
  requiredMemory: 12Gi
  limitedMemory: 12Gi
  requiredDisk: 10Gi
permission:
  appData: true
options:
  apiTimeout: 3600
  dependencies:
    - name: olares
      version: '>=1.12.2-0'
      type: system
  allowMultipleInstall: false
